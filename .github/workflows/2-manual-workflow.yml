name: Manual Workflow

on:
  workflow_dispatch:

jobs:
  first-workflow:
    runs-on: ubuntu-latest

    steps:
      - name: First step
        run: echo "Hello world!"

      - name: Secons step
        run: echo "Hello again"
